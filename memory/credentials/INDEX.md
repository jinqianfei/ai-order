# 凭证索引

**安全规则**：
- 不在本文件记录明文密码、密钥或 token。
- 只记录凭证所在位置、读取方式和用途。
- 如需新增凭证，优先使用 `.env` 或系统密钥管理工具。

| 凭证 | 用途 | 存放位置 | 读取方式 | 最后确认 |
|------|------|----------|----------|----------|
| `DB_PASSWORD` | AWS RDS PostgreSQL 连接密码 | `.env` 或环境变量 | `DB_PASSWORD` | 2026-06-16 |
| `OPENCLAW_FEISHU_WEBHOOK` | 日结/告警飞书推送 | `.env` 或环境变量 | `OPENCLAW_FEISHU_WEBHOOK` | 2026-06-16 |

